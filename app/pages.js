import React from "react";
import { Content } from "carbon-components-react";

const Page = ({ title }) => (
  <Content>
    <h1>{title}</h1>
    {/* Add your random Carbon components here */}
  </Content>
);

export default Page;
